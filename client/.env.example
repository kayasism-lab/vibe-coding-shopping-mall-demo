# Copy this file to `.env` and replace the placeholder values.
# Vite reads only variables prefixed with VITE_. Restart `npm run dev` after changes.
#
# Vercel 배포 시: Project Settings → Root Directory 를 "client" 로 두면
# 이 저장소의 client/vercel.json (SPA → index.html 리라이트)이 적용되어
# 토스 결제 후 /checkout/success , /checkout/fail 로 돌아올 때 404가 나지 않습니다.

# Backend API
# Local example: http://localhost:5000
# Production: 반드시 https:// 로 설정 (프론트가 HTTPS일 때 mixed content 차단 방지)
# Production example: https://your-api-domain
VITE_API_BASE_URL=http://localhost:5000

# Toss Payments — 클라이언트는 @tosspayments/tosspayments-sdk v2 사용 (Vite 빌드에 포함)
# 배포 환경 변수에도 동일하게 설정. 테스트/라이브 키는 토스 개발자센터에서 발급.
# 배포 도메인(정확한 origin, www 유무 포함)을 키에 등록해야 함.
# Test key example: test_ck_...
VITE_TOSS_CLIENT_KEY=your_toss_client_key

# Cloudinary Upload Widget (Search page)
# Cloud name: Cloudinary console → Dashboard → Account Details
# Upload preset: Settings → Upload → Upload presets → create unsigned preset for client-side upload
VITE_CLOUDINARY_CLOUD_NAME=your_cloud_name
VITE_CLOUDINARY_UPLOAD_PRESET=your_upload_preset_name
